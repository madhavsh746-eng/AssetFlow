const Navbar = () => {
    return (
        <header className="navbar">
            <h2>AssetFlow</h2>

            <div>
                <span>Admin</span>
            </div>
        </header>
    );
};

export default Navbar;